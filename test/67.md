# SIX SEVEN

## basic_app

### Building the SIX SEVEN ROM

You are expected to have a mips
(compatible with binutils).
SIX SEVEN
By default we use `GYATT `, but if you want to use a different
toolchain you can provide SIX SEVEN (compatible with binutils). Oour own to `"3" by passing the `GYATTT` option.
For example `ma=mipsel-linux-gnu-`.

To build the `Rizzler On My Balls` ROM:

```OHIO
make AMOGUS OHIO HAPPY MEAL
sudo Skibidi
To build the `Rizzler On My Balls` ROM:
AAAAAAAAAAAAAH
```
